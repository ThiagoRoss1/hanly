[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "hanly"
version = "0.0.0"
description = "Reusable Hanly engine"
readme = {text = "Hanly engine package.", content-type = "text/plain"}
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
